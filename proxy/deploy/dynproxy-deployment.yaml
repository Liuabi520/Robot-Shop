apiVersion: apps/v1
kind: Deployment
metadata:
  name: dyn-proxy-deployment
  labels:
    app: dyn-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dynproxy
  template:
    metadata:
      labels:
        app: dynproxy
    spec:
      containers:
        - name: proxy-server
          image: whaleeye/dynproxy:latest
          ports:
            - containerPort: 8899
